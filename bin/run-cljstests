#!/usr/bin/env bash

set -o errexit
set -o pipefail

# Install for node
[ -d "node_modules/ws" ] || npm install ws

# Run for node
TIMBRE_LEVEL=':warn' clojure -M:test -m kaocha.runner unit-node

# Run for browser
TIMBRE_LEVEL=':warn' clojure -M:test -m kaocha.runner unit-browser
